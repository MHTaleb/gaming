# Assets/_Game/Settings

- `Generated/` — created by `Glad to See You ▸ Bootstrap ▸ 1/2`:
  ScriptableObject data assets (PlayerStats, WeaponData, EnemyData, AbilityData, VFX, AudioEvent,
  Quality profiles), placeholder `.wav` SFX, and (optionally) the URP assets.
  These are **project assets** — commit them; tune them without touching code.
- `Input/` — mirror export of the code-built input actions (`GladToSeeYouControls.inputactions`).
  **The C# `InputActionLibrary` remains the single source of truth** — the mirror is for inspection only.
