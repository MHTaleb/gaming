# Independent validation requested

- Submission ID:
- Implementation branch / PR:
- Exact full implementation commit:
- Completed ticket IDs:
- Existing review IDs, if any:
- What changed and intended behavior:
- Commands, exit codes and results (separate spec/game/GPU/device checks):
- Sanitized evidence paths and hashes:
- Hardware / execution environment:
- Deferred services and capabilities:
- Known failures, skipped checks, provisional decisions:
- Review focus and downstream work that should wait:
- Next safe executor action:

Do not claim reviewer acceptance. Replace every blank with actual data or explicit not-applicable before submitting. Use response.json for findings from an existing review rather than creating a duplicate submission.
